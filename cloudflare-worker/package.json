{
  "name": "webglo-payment-backend",
  "version": "1.0.0",
  "description": "WebGlo Stripe payment backend on Cloudflare Workers",
  "main": "worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "stripe": "^14.0.0"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
